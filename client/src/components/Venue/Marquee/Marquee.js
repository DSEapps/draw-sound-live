import React from "react";


const Marquee = (props) => {
    return(
        <div className="marquee"> this is not being used, leaving in temporarily
      </div>
    );
}    
export default Marquee;
